@model IEnumerable<PhoneShop.Models.Category>

<ul>
	@if (Model != null && Model.Any())
	{
		@foreach (var category in Model)
		{
			<li><a href="#">@category.Name</a></li>
		}
	}
	else
	{
		<li><a href="#">No categories available</a></li>
	}
</ul>
